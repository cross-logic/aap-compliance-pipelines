app:
  title: AAP Compliance Pipelines
  baseUrl: http://localhost:3000

organization:
  name: AAP Compliance Prototype

backend:
  baseUrl: http://localhost:7007
  listen:
    port: 7007
  database:
    client: better-sqlite3
    connection: ':memory:'

# AAP Connection
# Override baseUrl and checkSSL in app-config.local.yaml (gitignored)
ansible:
  rhaap:
    baseUrl: ${AAP_BASE_URL}
    checkSSL: true
